Chatflow APP Frontend

- Follow instructions with the files in .clinerules
- Always get the latest backend specs. Is defined in http://127.0.0.1:8000/openapi.json
   - make sure you get this specs before writing anything
- create a Authentication flow and store token in persisten memory independently of the envoronment (android, web, linux, etc.)
- Create Ai chat application following specs that allows for text, audio, image, mcp calls messages
- side bar with user configuration + chat chistory
- Do not touch anything in the api folder (that is the backend)
